# 生产环境变量（复制为 .env 并填写真实值）
# 不要提交 .env 到 Git

# 会话密钥 — 用 openssl rand -hex 32 生成
SESSION_SECRET=change-me-in-production

# 允许的前端域名（CORS）
ALLOWED_ORIGIN=https://your.domain.com

# 数据库路径（容器内默认 /app/data/liushui.db 即可）
DATABASE_PATH=/app/data/liushui.db

# 端口
PORT=3001

# 环境
NODE_ENV=production
